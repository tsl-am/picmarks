<%= link_to "Add a New Picture", new_picture_path %>

<p>
  Here are some of my favorite photos
</p>

<ul>
<% @pictures.each do |p| %>
  <li>
    <%= link_to image_tag(p.url, :width => 60), picture_path(p.id) %>
    <p><%= p.title %><p>
  </li>
<% end %>
</ul>
