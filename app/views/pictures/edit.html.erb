<h2>Edit This Picture</h2>

<%= form_tag picture_url(@picture.id), :method => "put" do %>
  <%= label_tag :url, "URL:" %>
  <%= text_field_tag :url, @picture.url %>

  <%= label_tag :title, "Title:" %>
  <%= text_field_tag :title, @picture.title %>

  <%= submit_tag "Save"%>
  <%= link_to "or Cancel", pictures_url %>
<% end %>